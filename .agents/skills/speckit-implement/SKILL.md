---
name: speckit-implement
description: Enter implementation mode for a confirmed spec-kit feature while still respecting repository-specific standards.
---

# Spec Kit Implement

Use this skill as the implementation-stage handoff.

## Workflow

1. Confirm `spec.md` and, when needed, `plan.md` / `tasks.md` are already aligned.
2. Respect the confirmed implementation scope.
3. Route stack-specific work through `$project-stack-standards`.
4. Route final verification through `$project-test-and-report`.
